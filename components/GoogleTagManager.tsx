import Script from "next/script";

/**
 * Google Tag Manager. Container ID is public by design; override per
 * environment with NEXT_PUBLIC_GTM_ID if needed.
 */
const GTM_ID = process.env.NEXT_PUBLIC_GTM_ID || "GTM-MQZH3KTZ";

/** The GTM loader — Next injects it into <head> at hydration. */
export function GtmScript() {
  return (
    <Script id="gtm" strategy="afterInteractive">
      {`(function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':
new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],
j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src=
'https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);
})(window,document,'script','dataLayer','${GTM_ID}');`}
    </Script>
  );
}

/** The no-JS fallback — rendered immediately after the opening <body> tag. */
export function GtmNoScript() {
  return (
    <noscript>
      <iframe
        src={`https://www.googletagmanager.com/ns.html?id=${GTM_ID}`}
        height="0"
        width="0"
        style={{ display: "none", visibility: "hidden" }}
        title="Google Tag Manager"
      />
    </noscript>
  );
}
