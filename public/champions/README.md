# Champion headshots

Square photos (transparent or any background), one per champion:

- shawn-hendrix.jpg   <- pending upload (tile falls back to video thumbnail)
- cory-binks.jpg      <- optional

After uploading, set `championImage: "/champions/<file>"` on the story in
lib/stories.ts.
